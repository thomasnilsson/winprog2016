<Window x:Class="Diagram_WinProg2016.View.Arrows"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Diagram_WinProg2016.View"
        mc:Ignorable="d"
        Title="Arrows" Height="100" Width="100">
    <Grid>
        <Polyline Points="{Binding Points}" StrokeThickness="3">
            <Polyline.Style>
                <Style TargetType="{x:Type Polyline}">
                    <Setter Property="Stroke" Value="{Binding SelectedColor}" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Stroke" Value="Orange">
                            </Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Polyline.Style>
        </Polyline>
    </Grid>
</Window>
