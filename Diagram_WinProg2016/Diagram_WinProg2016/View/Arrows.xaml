<UserControl x:Class="Diagram_WinProg2016.View.Arrowss"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:cmd="clr-namespace:GalaSoft.MvvmLight.Command;assembly=GalaSoft.MvvmLight.Platform"
             >
    <Grid>
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="mouseClickArrow">
                <!--<cmd:EventToCommand Command="{Binding ArrowMouseBinder}"/>-->
            </i:EventTrigger>

        </i:Interaction.Triggers>


        <Polyline Points="{Binding Points}" StrokeThickness="3">
            <Polyline.Style>
                <Style TargetType="{x:Type Polyline}">
                    <Setter Property="Stroke" Value="{Binding SelectedColor}" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Stroke" Value="Blue">
                            </Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Polyline.Style>
        </Polyline>

    </Grid>
</UserControl>
