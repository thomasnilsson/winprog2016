<Window x:Class="Diagram_WinProg2016.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Diagram_WinProg2016.ViewModel"
        xmlns:Model="clr-namespace:Diagram_WinProg2016.Model"
        xmlns:local="clr-namespace:Diagram_WinProg2016"
        mc:Ignorable="d"
        DataContext="{Binding Source={StaticResource Locator}, Path=Main}"
        Title="Diagram App" Height="450" Width="720
        ">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:MainViewModel}">
            <Canvas>
                <local:ClassUserControl/>
            </Canvas>
        </DataTemplate>
    </Window.Resources>
   
    
    <DockPanel>
        
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Open Diagram" Command="{Binding OpenDiagram}"/>
                <MenuItem Header="_Save Diagram"/>
                <MenuItem Header="_Save as PNG" Command="{Binding SaveAsPngCommand}" InputGestureText="Ctrl+I" CommandParameter="{Binding ElementName=stack}" />

                <MenuItem Header="_Export to PDF"/>
                <MenuItem Header ="_Exit"/>
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Header="_Undo"/>
                <MenuItem Header="_Redo"/>
                <MenuItem Header="_Copy"/>
                <MenuItem Header="_Paste"/>
                <MenuItem Header="_Delete"/>
            </MenuItem>
            <MenuItem Header="_Insert">
                <MenuItem Header="_New Class" Command="{Binding AddClassCommand}"/>
                <MenuItem Header="_New Class" x:Name="insertclass2" Click="insertclass_Click"/>
                <MenuItem Header="_New Arrow"/>
            </MenuItem>
        </Menu>
        
        <Grid x:Name="MainGrid">
            <ItemsControl ItemsSource="{Binding Class}" Name="class" />
        </Grid>
        
    </DockPanel>
</Window>
